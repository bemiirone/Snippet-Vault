{
  "buildCommand": "npm install && npm exec -w web -- ng build && npm exec -w api -- tsc -p tsconfig.json",
  "outputDirectory": "web/dist/snippet-vault/browser",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "api/src/serverless.ts" }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}
