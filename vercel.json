{
  "buildCommand": "npm install && cd web && ../node_modules/.bin/ng build && cd .. && ./node_modules/.bin/tsc -p api/tsconfig.json",
  "outputDirectory": "web/dist/snippet-vault/browser",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "api/src/serverless.ts" }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}
